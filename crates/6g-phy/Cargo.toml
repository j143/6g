[package]
name = "sixg-phy"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sixg-common = { path = "../6g-common" }
sixg-ai     = { path = "../6g-ai" }
thiserror.workspace = true
serde.workspace = true

[features]
## Gate Level-2 baseline comparison tests (require inline reference data).
## Run with: cargo test -p sixg-phy --features=baseline-comparison
baseline-comparison = []

[dev-dependencies]
